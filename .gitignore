# Build artifacts
build/
*.o
*.a

# IDE files
.vscode/
.idea/
*.user

# Compiled binaries (no extension, Unix executables)
# Exclude compiled test binaries but NOT test source files (test_*.cpp)
/pablo

# Auto-generated knowledge base in data dir (user data)
# Keep the default data/pablo.kb in source control but not the build copy
build/
